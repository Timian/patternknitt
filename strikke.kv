#strikke.kv is the ui / design for the developed application.

#:kivy 1.0.9
MainScreenManager:
    Strikkeapp:

<Strikkeapp>:
#The main box where the buttons are placed in.
#Orientation gives hos the orientation on the buttons should be
#buttons are the buttons they can push to activate some kind of function. 
#every buttons has it name based out of the main function in it.
    BoxLayout:              
        orientation: "vertical"
        Button:
            background_color: ( 0.7, 0.4, 0, 1)
            text: "textbasert"
        Button:
            background_color: ( 0.8, 0.5, 0, 1)
            text: "Pattern basert"
        Button:
            background_color: ( 0.9, 0.6, 0, 1)
            text: "Pattern og Text basert"



